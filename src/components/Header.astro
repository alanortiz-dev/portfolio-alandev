---
import SettingsIcon from './icons/SettingsIcon.astro';
import LanguageToggle from './LanguageToggle.astro';
import ThemeToggle from './ThemeToggle.astro';
import { homeContent } from '@/data/homeContent';

const currentLang = (Astro.currentLocale || 'es') as 'es' | 'en';
const content = homeContent[currentLang];
const settingsMenuId = 'site-settings-menu';
const cvHref = currentLang === 'en' ? '/docs/Alan_Ortiz_EN.pdf' : '/docs/Alan_Ortiz_ES.pdf';
const cvDownload = currentLang === 'en' ? 'Alan_Ortiz_CV_EN.pdf' : 'Alan_Ortiz_CV_ES.pdf';
const cvLabel = currentLang === 'en' ? 'Download CV' : 'Descargar CV';
---

<header class="sticky top-0 z-50 border-b border-slate-200/80 bg-white/80 backdrop-blur-xl dark:border-white/10 dark:bg-slate-950/80">
  <nav class="mx-auto flex w-full max-w-7xl items-center justify-between gap-4 px-4 py-3 sm:px-6 lg:px-8 text-sm text-slate-700 dark:text-slate-200">
    <a href={currentLang === 'en' ? '/en' : '/es'} class="inline-flex items-center gap-3 font-semibold tracking-tight text-slate-950 dark:text-white">
      <span class="inline-flex size-9 items-center justify-center rounded-xl border border-emerald-400/30 bg-emerald-400/10 text-emerald-700 dark:text-emerald-300">AO</span>
      <span>Alan Ortiz</span>
    </a>

    <div class="hidden items-center gap-7 md:flex">
      {[
        { label: currentLang === 'en' ? 'Home' : 'Inicio', url: '#top' },
        { label: currentLang === 'en' ? 'About' : 'Sobre mí', url: '#about' },
        { label: content.nav.featured, url: '#featured-engineering' },
        { label: currentLang === 'en' ? 'Skills' : 'Habilidades', url: '#engineering-principles' },
        { label: content.nav.timeline, url: '#experience-timeline' },
        { label: content.nav.contact, url: 'mailto:alan.omar.ortz@gmail.com' },
      ].map((link) => (
        <a class="transition hover:text-slate-950 dark:hover:text-white" href={link.url}>{link.label}</a>
      ))}
    </div>

    <div class="flex items-center gap-2 sm:gap-3">
      <a
        href={cvHref}
        download={cvDownload}
        class="hidden min-h-10 items-center justify-center rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-800 transition hover:border-slate-300 hover:bg-slate-50 focus:outline-none focus-visible:ring focus-visible:ring-slate-300 focus-visible:ring-offset-2 sm:inline-flex dark:border-white/10 dark:bg-white/[0.03] dark:text-slate-100 dark:hover:bg-white/[0.06]"
      >
        {cvLabel}
      </a>

      <div class="relative flex items-center gap-2" data-settings-root>
        <button
          type="button"
          data-settings-button
          aria-label={currentLang === 'en' ? 'Open site settings' : 'Abrir ajustes del sitio'}
          aria-expanded="false"
          aria-controls={settingsMenuId}
          class="rounded-full border border-slate-200 p-2 text-slate-600 transition hover:text-slate-950 focus:outline-none focus-visible:ring focus-visible:ring-slate-300 focus-visible:ring-offset-2 dark:border-white/10 dark:text-slate-300 dark:hover:text-white"
        >
          <SettingsIcon class="h-5 w-5" />
        </button>

        <div
          id={settingsMenuId}
          data-settings-menu
          class="absolute right-0 top-11 z-50 hidden w-44 rounded-2xl border border-slate-200 bg-white p-2 shadow-lg shadow-slate-200/40 dark:border-white/10 dark:bg-slate-900"
        >
          <LanguageToggle />
          <ThemeToggle />
        </div>
      </div>
    </div>
  </nav>
</header>

<style>
  [data-settings-menu].open {
    display: block;
    animation: scale-up-center 0.15s ease-out both;
  }

  @keyframes scale-up-center {
    from {
      transform: scale(0.8);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }
</style>

<script is:inline>
  const root = document.querySelector('[data-settings-root]');
  const btn = root?.querySelector('[data-settings-button]');
  const menu = root?.querySelector('[data-settings-menu]');

  const closeMenu = () => {
    menu?.classList.remove('open');
    btn?.setAttribute('aria-expanded', 'false');
  };

  btn?.addEventListener('click', (e) => {
    e.stopPropagation();
    const isOpen = menu?.classList.contains('open');
    if (isOpen) {
      closeMenu();
      return;
    }

    menu?.classList.add('open');
    btn?.setAttribute('aria-expanded', 'true');
  });

  document.addEventListener('click', () => closeMenu());
  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') closeMenu();
  });
  document.addEventListener('astro:after-swap', () => closeMenu());
</script>
