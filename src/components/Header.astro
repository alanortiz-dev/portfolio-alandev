---
import SettingsIcon from './icons/SettingsIcon.astro';
import LanguageToggle from './LanguageToggle.astro';
import ThemeToggle from './ThemeToggle.astro';
import { homeContent } from '@/data/homeContent';

const currentLang = (Astro.currentLocale || 'es') as 'es' | 'en';
const content = homeContent[currentLang];
---

<header class="sticky top-0 z-50 border-b border-slate-200/80 bg-white/85 backdrop-blur-xl dark:border-white/10 dark:bg-slate-950/80">
  <nav class="mx-auto flex w-full max-w-6xl items-center justify-between gap-4 px-4 py-3 text-sm text-slate-700 dark:text-slate-200">
    <a href={currentLang === 'en' ? '/en' : '/es'} class="font-semibold tracking-tight text-slate-950 dark:text-white">
      Alan Ortiz
    </a>

    <div class="hidden items-center gap-5 md:flex">
      {[
        { label: content.nav.featured, url: '#featured-engineering' },
        { label: content.nav.experience, url: '#production-experience' },
        { label: content.nav.deployments, url: '#production-deployments' },
        { label: content.nav.principles, url: '#engineering-principles' },
        { label: content.nav.timeline, url: '#experience-timeline' },
      ].map((link) => (
        <a class="transition hover:text-slate-950 dark:hover:text-white" href={link.url}>{link.label}</a>
      ))}
      <a class="transition hover:text-slate-950 dark:hover:text-white" href="mailto:alan.omar.ortz@gmail.com">{content.nav.contact}</a>
    </div>

    <div class="relative ml-2 flex items-center gap-2">
      <button id="settings-btn" class="rounded-full border border-slate-200 p-2 text-slate-600 transition hover:text-slate-950 dark:border-white/10 dark:text-slate-300 dark:hover:text-white">
        <SettingsIcon class="h-5 w-5" />
      </button>

      <div
        id="settings-menu"
        class="absolute right-0 top-11 z-50 hidden w-44 rounded-2xl border border-slate-200 bg-white p-2 shadow-lg shadow-slate-200/40 dark:border-white/10 dark:bg-slate-900"
      >
        <LanguageToggle />
        <ThemeToggle />
      </div>
    </div>
  </nav>
</header>

<style>
  #settings-menu.open {
    display: block;
    animation: scale-up-center 0.15s ease-out both;
  }

  @keyframes scale-up-center {
    from {
      transform: scale(0.8);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }
</style>

<script is:inline>
  const btn = document.getElementById('settings-btn');
  const menu = document.getElementById('settings-menu');

  btn?.addEventListener('click', (e) => {
    e.stopPropagation();
    const isOpen = menu.classList.contains('open');
    document.querySelectorAll('#settings-menu').forEach((m) => m.classList.remove('open'));
    if (!isOpen) menu.classList.add('open');
  });

  document.addEventListener('click', () => menu?.classList.remove('open'));
  document.addEventListener('astro:after-swap', () => {
    menu?.classList.remove('open');
  });
</script>
